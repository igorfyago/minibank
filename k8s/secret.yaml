# The database password as a Secret · the manifests reference it by name so
# the credential is never baked into an image or a Deployment spec. (Demo
# value; a real cluster sources this from a sealed secret or a cloud KMS.)
apiVersion: v1
kind: Secret
metadata:
  name: minibank-db
type: Opaque
stringData:
  password: minibank
